name: wtaiass
description: War Thunder AI Assistant Overlay - Tactical Map & AI Wingman
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0
  provider: ^6.1.2
  shared_preferences: ^2.3.0
  wakelock_plus: ^1.2.0
  sqflite: ^2.3.3
  path: ^1.9.0
  uuid: ^4.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true