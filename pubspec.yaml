name: wt_copilot
description: Modular AI Wingman for War Thunder
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0
  provider: ^6.1.2
  shared_preferences: ^2.2.2
  wakelock_plus: ^1.1.1
  sqflite: ^2.3.2
  path: ^1.8.3
  logger: ^2.0.2
  package_info_plus: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true